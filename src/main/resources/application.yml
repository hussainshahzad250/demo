server:
  port: 7060

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev?useSSL=false
    username: root
    password: P@ssw0rd
  jpa:
    show-sql: false
    properties:
     hibernate:
     dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      max-file-size: 6MB
